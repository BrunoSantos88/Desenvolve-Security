console.log(apresentar()); // Deve exibir "olá"

function apresentar() {
  return "olá";
}

// A função soma deve ser definida antes de ser chamada
const soma = function(num1, num2) {
  return num1 + num2;
};

console.log(soma(1, 1)); // Deve exibir 2